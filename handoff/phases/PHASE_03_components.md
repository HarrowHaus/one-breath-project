# Phase 3 — Component library

**Produces:** the reusable building blocks for every page.
**You need first:** nothing.

## Instruction for Claude Code

```
Build a reusable component library on USWDS, each piece accessible and responsive:
hero (full-bleed, NO stats inside it), a three-card "audience doors" group, summary box, alert (with seasonal info/warning variants), step indicator, process list, accordion, a "Measured/Modeled" tag, form controls (text, textarea with character count, radio, select), a range slider, and a print-friendly checklist. Use /docs/01_VOICE_GUIDE.md for any placeholder text. Show me a components preview page on the live site. On success, hand the commit to the git-committer sub-agent (push to main, no PR).
```

## Done when
A preview page shows each component rendering correctly on desktop and mobile.
